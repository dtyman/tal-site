---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About | Tyman Auerbach Attorneys at Law">
  <section class="content-section">
    <div class="section-inner-narrow">
      <h1 class="section-title">About Us</h1>
      <p class="lead-text" style="margin-bottom: 2rem;">
        We'll build out the detailed about page with full bios and the firm story next. For now, you can see the intro on the homepage.
      </p>
      <a href="/" class="btn btn-primary">Back to Home</a>
    </div>
  </section>
</BaseLayout>

<style>
  .lead-text {
    font-family: 'Crimson Pro', serif;
    font-size: 1.5rem;
    line-height: 1.5;
    color: var(--charcoal);
  }

  .btn {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 1.2rem;
    letter-spacing: 0.5px;
    text-decoration: none;
    padding: 1rem 2.2rem;
    border-radius: 2px;
    transition: all 0.2s ease;
    display: inline-block;
  }

  .btn-primary {
    background: var(--red);
    color: var(--warm-white);
    box-shadow: 4px 4px 0 var(--warm-black);
    font-weight: 600;
  }

  .btn-primary:hover {
    background: var(--deep-red);
    transform: translate(3px, 3px);
    box-shadow: 1px 1px 0 var(--warm-black);
  }
</style>
